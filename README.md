# Bookmark Manager

Private read-later bookmark manager take-home.

## Setup

Setup TBD (monorepo, Docker Postgres, Auth0, and run scripts land in Step 2).

## Auth token

API Bearer uses the Auth0 access token (not the ID token); rationale and ADR — see [DECISIONS.md](./DECISIONS.md).
